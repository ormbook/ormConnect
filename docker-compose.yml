version: '3.8'

services:
  ormconnect:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ormconnect-app
    ports:
      - "3000:3000"
    restart: always
    environment:
      - PORT=3000
      - NODE_ENV=production
